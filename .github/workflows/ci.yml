name: CI

on:
  push:
    branches: ["main", "dev"]
  pull_request:
    branches: ["main"]

jobs:
  build:
    name: Build (Windows / zig cc)
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Zig
        uses: mlugg/setup-zig@v1
        with:
          version: "0.16.0"

      - name: Build
        run: .\build.bat

      - name: Upload binary
        uses: actions/upload-artifact@v4
        with:
          name: satiscli-${{ github.sha }}
          path: build\myshell.exe
          if-no-files-found: error
