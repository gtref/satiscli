# SATISCLI
A simple, easy and elegant alternate to windows command prompter and powershell.

## Using
Satiscli is a simple bash like shell for windows. It has a small number of built in commands and functions such as `pwd`, `cd`, `ls`, `echo`, `set` and support for the `.satiscli` config file.

```bash
winget install --id zig.zig
```
